# Module 2 Assignment
The code provided by the assignment uses the same name as a variable for the method's parameter name, and doesn't reference the parameter name within the `myMean` function.

After rewriting the function to replace `assignment2` with `dataVec`, then replacing `assignment` and `someData` with `dataVec`, it no longer produces an error and now functions as intended.